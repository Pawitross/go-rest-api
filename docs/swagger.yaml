basePath: /api/v1
definitions:
  Author:
    properties:
      birth_year:
        type: integer
      death_year:
        type: integer
      first_name:
        type: string
      id:
        type: integer
      last_name:
        type: string
    type: object
  Book:
    properties:
      author:
        type: integer
      genre:
        type: integer
      id:
        type: integer
      language:
        type: integer
      pages:
        type: integer
      title:
        type: string
      year:
        type: integer
    type: object
  ErrorResponse:
    properties:
      error:
        type: string
    type: object
  Genre:
    properties:
      id:
        type: integer
      name:
        type: string
    type: object
  Language:
    properties:
      id:
        type: integer
      name:
        type: string
    type: object
  TokenRequest:
    properties:
      return_admin_token:
        type: boolean
    required:
    - return_admin_token
    type: object
  TokenResponse:
    properties:
      admin:
        type: boolean
      token:
        type: string
    type: object
externalDocs:
  description: OpenAPI Specification
  url: https://swagger.io/resources/open-api/
info:
  contact: {}
  description: |-
    Documentation of a book managing REST API.

    **How to use filtering:**
    To use simple filtering put name of the column in the query parameter followed by the value.
    Examples: `last_name=Orwell`, `title=Dziady`
    To filter extended response use filtering like this: `genre.name=Nowela`

    To filter using comparison operators append the operator to the query parameter. Available operators:
    - less than = `.lt`
    - less than or equal = `.lte`
    - greater than = `.gt`
    - greater than or equal = `.gte`
    - equal = `.eq`
    - not equal = `.neq`

    Examples: `pages.lt=300`, `year.gte=1980`, `language.name.neq=Polski`.

    **How to use sorting:**
    To sort, use `sort_by` query parameter followed by the column name.
    If you want to sort in descending order, prefix the column name with a minus sign (`-`).
    Examples: `sort_by=pages` - ascending order, `sort_by=-pages` - descending order

    **How to use limit and offset:**
    To use limit, use the `limit` query parameter, like this: `limit=10`
    To use offset, you also need to provide a limit.
    The order of the limit and offset parameters doesn't matter.
    Examples: `offset=10&limit=50`, `limit=50&offset=10`
  title: Book managing API
paths:
  /authors:
    get:
      description: Responds with a list of all authors as JSON. Optional filtering,
        sorting and pagination is available through parameters.
      parameters:
      - description: Author id
        in: query
        name: id
        type: string
      - description: Author's first name
        in: query
        name: first_name
        type: string
      - description: Author's last name
        in: query
        name: last_name
        type: string
      - description: Author's birth year
        in: query
        name: birth_year
        type: integer
      - description: Author's death year
        in: query
        name: death_year
        type: string
      - description: Sorting by a column
        in: query
        name: sort_by
        type: string
      - description: Limit returned number of resources
        in: query
        name: limit
        type: integer
      - description: Offset returned resources
        in: query
        name: offset
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK - Fetched authors
          schema:
            items:
              $ref: '#/definitions/Author'
            type: array
        "400":
          description: Bad Request - Invalid input
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Get a list of all authors
      tags:
      - Authors
    options:
      description: Responds with an empty response body.
      responses:
        "204":
          description: No Content - Successfully responded with available options
          headers:
            Allow:
              description: Allowed operations for the resource
              type: string
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Return allowed operations for authors
      tags:
      - Authors
    post:
      consumes:
      - application/json
      description: Accepts a JSON body to create a new author. Responds with the created
        author and set `Location` header or an error message.
      parameters:
      - description: New Author
        in: body
        name: author
        required: true
        schema:
          $ref: '#/definitions/Author'
      produces:
      - application/json
      responses:
        "201":
          description: Created - Added new author
          headers:
            Location:
              description: Path of the newly created author
              type: string
          schema:
            $ref: '#/definitions/Author'
        "400":
          description: Bad Request - Invalid input or JSON
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Create a new author
      tags:
      - Authors
  /authors/{id}:
    delete:
      description: Responds with a status code. When an error occurs the response
        body contains an error message.
      parameters:
      - description: Author id
        in: path
        name: id
        required: true
        type: integer
      responses:
        "204":
          description: No Content - Successfully deleted the author
        "400":
          description: Bad Request - Invalid author id
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
        "404":
          description: Not Found -  No resource found
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Delete an existing author
      tags:
      - Authors
    get:
      description: Responds with the queried author as JSON or an error message.
      parameters:
      - description: Author id
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK - Fetched author
          schema:
            $ref: '#/definitions/Author'
        "400":
          description: Bad Request - Invalid author id
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "404":
          description: Not Found - No resource found
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Get one author
      tags:
      - Authors
    options:
      description: Responds with an empty response body.
      parameters:
      - description: Author id
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content - Successfully responded with available options
          headers:
            Allow:
              description: Allowed operations for the resource
              type: string
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Return allowed operations for author
      tags:
      - Authors
    patch:
      consumes:
      - application/json
      description: Accepts a JSON body with patch data to a author. Responds with
        a status code. When an error occurs the response body contains JSON data with
        the message.
      parameters:
      - description: Existing Author id
        in: path
        name: id
        required: true
        type: integer
      - description: Patches to the author
        in: body
        name: author
        required: true
        schema:
          $ref: '#/definitions/Author'
      responses:
        "204":
          description: No Content - Successfully patched the author
        "400":
          description: Bad Request - Invalid input or JSON
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
        "404":
          description: Not Found -  No resource found
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Patch an existing author
      tags:
      - Authors
    put:
      consumes:
      - application/json
      description: Accepts a JSON body to update a author. Responds with a status
        code. When an error occurs the response body contains JSON data with the message.
      parameters:
      - description: Existing Author id
        in: path
        name: id
        required: true
        type: integer
      - description: Updated Author
        in: body
        name: author
        required: true
        schema:
          $ref: '#/definitions/Author'
      responses:
        "204":
          description: No content - Updated the author
        "400":
          description: Bad Request - Invalid input or JSON
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
        "404":
          description: Not Found -  No resource found
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Update an existing author
      tags:
      - Authors
  /books:
    get:
      description: Responds with a list of all books as JSON. Optional filtering,
        sorting and pagination is available through parameters.
      parameters:
      - description: Book id
        in: query
        name: id
        type: string
      - description: Book title
        in: query
        name: title
        type: string
      - description: Year of publishing of the book
        in: query
        name: year
        type: integer
      - description: Number of pages in the book
        in: query
        name: pages
        type: integer
      - description: Author id
        in: query
        name: author
        type: integer
      - description: Genre id
        in: query
        name: genre
        type: integer
      - description: Language id
        in: query
        name: language
        type: integer
      - description: Sorting by a column
        in: query
        name: sort_by
        type: string
      - description: Limit returned number of resources
        in: query
        name: limit
        type: integer
      - description: Offset returned resources
        in: query
        name: offset
        type: integer
      - description: Return extended book information
        in: query
        name: extend
        type: boolean
      - description: If extend=true - Author id
        in: query
        name: author.id
        type: integer
      - description: If extend=true - Author first name
        in: query
        name: author.first_name
        type: string
      - description: If extend=true - Author last name
        in: query
        name: author.last_name
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK - Fetched books
          schema:
            items:
              $ref: '#/definitions/Book'
            type: array
        "400":
          description: Bad Request - Invalid input
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Get a list of all books
      tags:
      - Books
    options:
      description: Responds with an empty response body.
      responses:
        "204":
          description: No Content - Successfully responded with available options
          headers:
            Allow:
              description: Allowed operations for the resource
              type: string
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Return allowed operations for books
      tags:
      - Books
    post:
      consumes:
      - application/json
      description: Accepts a JSON body to create a new book. Responds with the created
        book and set `Location` header or an error message.
      parameters:
      - description: New Book
        in: body
        name: book
        required: true
        schema:
          $ref: '#/definitions/Book'
      produces:
      - application/json
      responses:
        "201":
          description: Created - Added new book
          headers:
            Location:
              description: Path of the newly created book
              type: string
          schema:
            $ref: '#/definitions/Book'
        "400":
          description: Bad Request - Invalid input or JSON
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Create a new book
      tags:
      - Books
  /books/{id}:
    delete:
      description: Responds with a status code. When an error occurs the response
        body contains an error message.
      parameters:
      - description: Book id
        in: path
        name: id
        required: true
        type: integer
      responses:
        "204":
          description: No Content - Successfully deleted the book
        "400":
          description: Bad Request - Invalid book id
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
        "404":
          description: Not Found -  No resource found
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Delete an existing book
      tags:
      - Books
    get:
      description: Responds with the queried book as JSON or an error message.
      parameters:
      - description: Book id
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK - Fetched book
          schema:
            $ref: '#/definitions/Book'
        "400":
          description: Bad Request - Invalid book id
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "404":
          description: Not Found - No resource found
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Get one book
      tags:
      - Books
    options:
      description: Responds with an empty response body.
      parameters:
      - description: Book id
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content - Successfully responded with available options
          headers:
            Allow:
              description: Allowed operations for the resource
              type: string
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Return allowed operations for books
      tags:
      - Books
    patch:
      consumes:
      - application/json
      description: Accepts a JSON body with patch data to a book. Responds with a
        status code. When an error occurs the response body contains JSON data with
        the message.
      parameters:
      - description: Existing Book id
        in: path
        name: id
        required: true
        type: integer
      - description: Patches to the book
        in: body
        name: book
        required: true
        schema:
          $ref: '#/definitions/Book'
      responses:
        "204":
          description: No Content - Successfully patched the book
        "400":
          description: Bad Request - Invalid input or JSON
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
        "404":
          description: Not Found -  No resource found
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Patch an existing book
      tags:
      - Books
    put:
      consumes:
      - application/json
      description: Accepts a JSON body to update a book. Responds with a status code.
        When an error occurs the response body contains JSON data with the message.
      parameters:
      - description: Existing Book id
        in: path
        name: id
        required: true
        type: integer
      - description: Updated Book
        in: body
        name: book
        required: true
        schema:
          $ref: '#/definitions/Book'
      responses:
        "204":
          description: No content - Updated the book
        "400":
          description: Bad Request - Invalid input or JSON
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
        "404":
          description: Not Found -  No resource found
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Update an existing book
      tags:
      - Books
  /genres:
    get:
      description: Responds with a list of all genres as JSON. Optional filtering,
        sorting and pagination is available through parameters.
      parameters:
      - description: Genre id
        in: query
        name: id
        type: string
      - description: Genre name
        in: query
        name: name
        type: string
      - description: Sorting by a column
        in: query
        name: sort_by
        type: string
      - description: Limit returned number of resources
        in: query
        name: limit
        type: integer
      - description: Offset returned resources
        in: query
        name: offset
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK - Fetched genres
          schema:
            items:
              $ref: '#/definitions/Genre'
            type: array
        "400":
          description: Bad Request - Invalid input
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Get a list of all genres
      tags:
      - Genres
    options:
      description: Responds with an empty response body.
      responses:
        "204":
          description: No Content - Successfully responded with available options
          headers:
            Allow:
              description: Allowed operations for the resource
              type: string
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Return allowed operations for genres
      tags:
      - Genres
    post:
      consumes:
      - application/json
      description: Accepts a JSON body to create a new genre. Responds with the created
        genre and set `Location` header or an error message.
      parameters:
      - description: New Genre
        in: body
        name: genre
        required: true
        schema:
          $ref: '#/definitions/Genre'
      produces:
      - application/json
      responses:
        "201":
          description: Created - Added new genre
          headers:
            Location:
              description: Path of the newly created genre
              type: string
          schema:
            $ref: '#/definitions/Genre'
        "400":
          description: Bad Request - Invalid input or JSON
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Create a new genre
      tags:
      - Genres
  /genres/{id}:
    delete:
      description: Responds with a status code. When an error occurs the response
        body contains an error message.
      parameters:
      - description: Genre id
        in: path
        name: id
        required: true
        type: integer
      responses:
        "204":
          description: No Content - Successfully deleted the genre
        "400":
          description: Bad Request - Invalid genre id
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
        "404":
          description: Not Found -  No resource found
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Delete an existing genre
      tags:
      - Genres
    get:
      description: Responds with the queried genre as JSON or an error message.
      parameters:
      - description: Genre id
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK - Fetched genre
          schema:
            $ref: '#/definitions/Genre'
        "400":
          description: Bad Request - Invalid genre id
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "404":
          description: Not Found - No resource found
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Get one genre
      tags:
      - Genres
    options:
      description: Responds with an empty response body.
      parameters:
      - description: Genre id
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content - Successfully responded with available options
          headers:
            Allow:
              description: Allowed operations for the resource
              type: string
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Return allowed operations for genres
      tags:
      - Genres
    put:
      consumes:
      - application/json
      description: Accepts a JSON body to update a genre. Responds with a status code.
        When an error occurs the response body contains JSON data with the message.
      parameters:
      - description: Existing Genre id
        in: path
        name: id
        required: true
        type: integer
      - description: Updated Genre
        in: body
        name: genre
        required: true
        schema:
          $ref: '#/definitions/Genre'
      responses:
        "204":
          description: No content - Updated the genre
        "400":
          description: Bad Request - Invalid input or JSON
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
        "404":
          description: Not Found -  No resource found
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Update an existing genre
      tags:
      - Genres
  /languages:
    get:
      description: Responds with a list of all languages as JSON. Optional filtering,
        sorting and pagination is available through parameters.
      parameters:
      - description: Language id
        in: query
        name: id
        type: string
      - description: Language name
        in: query
        name: name
        type: string
      - description: Sorting by a column
        in: query
        name: sort_by
        type: string
      - description: Limit returned number of resources
        in: query
        name: limit
        type: integer
      - description: Offset returned resources
        in: query
        name: offset
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK - Fetched languages
          schema:
            items:
              $ref: '#/definitions/Language'
            type: array
        "400":
          description: Bad Request - Invalid input
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Get a list of all languages
      tags:
      - Languages
    options:
      description: Responds with an empty response body.
      responses:
        "204":
          description: No Content - Successfully responded with available options
          headers:
            Allow:
              description: Allowed operations for the resource
              type: string
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Return allowed operations for languages
      tags:
      - Languages
    post:
      consumes:
      - application/json
      description: Accepts a JSON body to create a new language. Responds with the
        created language and set `Location` header or an error message.
      parameters:
      - description: New Language
        in: body
        name: language
        required: true
        schema:
          $ref: '#/definitions/Language'
      produces:
      - application/json
      responses:
        "201":
          description: Created - Added new language
          headers:
            Location:
              description: Path of the newly created language
              type: string
          schema:
            $ref: '#/definitions/Language'
        "400":
          description: Bad Request - Invalid input or JSON
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Create a new language
      tags:
      - Languages
  /languages/{id}:
    delete:
      description: Responds with a status code. When an error occurs the response
        body contains an error message.
      parameters:
      - description: Language id
        in: path
        name: id
        required: true
        type: integer
      responses:
        "204":
          description: No Content - Successfully deleted the language
        "400":
          description: Bad Request - Invalid language id
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
        "404":
          description: Not Found -  No resource found
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Delete an existing language
      tags:
      - Languages
    get:
      description: Responds with the queried language as JSON or an error message.
      parameters:
      - description: Language id
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK - Fetched language
          schema:
            $ref: '#/definitions/Language'
        "400":
          description: Bad Request - Invalid language id
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "404":
          description: Not Found - No resource found
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Get one language
      tags:
      - Languages
    options:
      description: Responds with an empty response body.
      parameters:
      - description: Language id
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content - Successfully responded with available options
          headers:
            Allow:
              description: Allowed operations for the resource
              type: string
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Return allowed operations for languages
      tags:
      - Languages
    put:
      consumes:
      - application/json
      description: Accepts a JSON body to update a language. Responds with a status
        code. When an error occurs the response body contains JSON data with the message.
      parameters:
      - description: Existing Language id
        in: path
        name: id
        required: true
        type: integer
      - description: Updated Language
        in: body
        name: language
        required: true
        schema:
          $ref: '#/definitions/Language'
      responses:
        "204":
          description: No content - Updated the language
        "400":
          description: Bad Request - Invalid input or JSON
          schema:
            $ref: '#/definitions/ErrorResponse'
        "401":
          description: Unauthorized - Invalid or missing token
          schema:
            $ref: '#/definitions/ErrorResponse'
        "403":
          description: Forbidden - Insufficient permissions
          schema:
            $ref: '#/definitions/ErrorResponse'
        "404":
          description: Not Found -  No resource found
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/ErrorResponse'
      security:
      - ApiKeyAuth: []
      summary: Update an existing language
      tags:
      - Languages
  /login:
    post:
      description: |-
        Return a valid JWT token used for authentication and authorization. Token expires after 30 minutes.
        Endpoint requires a JSON request body with a `return_admin_token` boolean field. Setting it to `true` returns an admin access token.
      parameters:
      - description: Return an admin token
        in: body
        name: admin
        required: true
        schema:
          $ref: '#/definitions/TokenRequest'
      responses:
        "200":
          description: OK - Response body contains JWT token
          schema:
            $ref: '#/definitions/TokenResponse'
        "400":
          description: Bad Request - Invalid parameter value
          schema:
            $ref: '#/definitions/ErrorResponse'
        "500":
          description: Internal Server Error - Failed to create JWT token
          schema:
            $ref: '#/definitions/ErrorResponse'
      summary: Get a JWT token
      tags:
      - Auth
securityDefinitions:
  ApiKeyAuth:
    description: |-
      Provide the JWT token as a Bearer token in the format "Bearer <your_token_here>".
      To get the token use the /login endpoint with necessary body.
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
